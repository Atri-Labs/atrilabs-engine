export function CanvasZone(props: { id: string }) {
  // TODO
  // useLocation to get location & route
  // use the route to get the data from store
  // use the callbacks to hydrate
  return <div>Canvas Zone</div>;
}
