import { container, menu } from "@atrilabs/core";

export const appMenu = menu("AppMenu")!;
export const dropContainer = container("Drop")!;
export const overlayContainer = container("OverlayContainer")!;
